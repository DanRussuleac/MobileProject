<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/Background">

    <!-- ListView to display workout logs -->
    <ListView
        android:id="@+id/lvWorkoutLog"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"
        android:padding="8dp"
        android:clipToPadding="false"
        android:scrollbars="vertical"/>

    <!-- EditText for inputting the muscle group -->
    <EditText
        android:id="@+id/etMuscle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Muscle"
        android:layout_marginTop="16dp"/>

    <!-- EditText for inputting exercises -->
    <EditText
        android:id="@+id/etExercises"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Exercises"
        android:layout_marginTop="16dp"/>

    <!-- Button for adding a new workout entry -->
    <Button
        android:id="@+id/btnAddWorkout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add New Workout"
        android:textColor="#FFFFFF"
        android:background="@drawable/button_rounded"
        android:layout_marginTop="16dp"/>

</LinearLayout>
